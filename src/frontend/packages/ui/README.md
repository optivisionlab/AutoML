# UI Package

Shared UI can live here later if a component is intentionally cross-app.

For now, web-only UI remains in `apps/web/src/shared/components` because React DOM and React Native UI are not interchangeable.
