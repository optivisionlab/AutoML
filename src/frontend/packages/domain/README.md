# Domain Package

Platform-neutral business logic should live here when the web and mobile apps need to share it.

Examples:

- Training configuration validation.
- Dataset/model types and mappers.
- API request/response schemas that do not depend on React DOM.
